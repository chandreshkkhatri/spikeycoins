services:
  - type: web
    name: open-mandi-express-backend
    runtime: node
    rootDir: web-server
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8000
